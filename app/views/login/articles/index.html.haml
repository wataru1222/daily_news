%p#notice= notice
%h1 "My Articles"
%table.table.table-striped
  %thead
    %tr
      %th Title
      %th Content
      %th{colspan: "3"}
  %tbody
    - @articles.each do |article|
      %tr
        %td= article.title
        %td= article.content
        %td= link_to 'Show', [:login, article]
        %td= link_to 'Edit', edit_login_article_path(article)
        %td= link_to 'Destroy', [login,article], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Article', new_login_article_path
